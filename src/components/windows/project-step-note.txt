# used react-rnd to use draggable and resizeable 
# added define: {
    'process.env': {}
  } --> cause draggbale was not working

  #  <Rnd
            default={{
                x: 50,
                y: 50,
                width: "40vw",
                height: "40vh"
            }} >
        in window--> gave h100%
       .card{ flex-grow: 1;
        flex-basis: 15rem;
        max-width: 25rem; }

#logic to render only if link present-->    {data.demoLink && <a href={data.demoLink}>demoLink</a>}


used -- > import ReactMarkdown from "react-markdown"
 & wrapped {markdown} in <ReactMarkdown>

 used --> npm i react-syntax-highlighter
 added css to note  & added scrollbar to windows 
 

 Created Resume section and integrated it into the application.
- Added iframe/embed support to display PDF documents.
- Applied the following styles to prevent unwanted page scrolling:

  main {
    overflow: hidden;
    position: relative;
  }

- Ensured the application remains fixed within the viewport without showing scrollbars.

h100% in windows -->resizable in height

add spotify --> share-->embed link--
changes to be made to add spotify
## style= borderRadius: "12px"  -----> style={{ borderRadius: "12px" }} 
## allowfullscreen="" -----> allowfullscreen



### Terminal Window

- Added a new Terminal application window.
- Installed and integrated the `react-console-emulator` package.
- Investigated rendering issues caused by incorrect module exports.
- Discovered that the package was exporting an object instead of a React component.
- Used the package's `default` export to correctly render the terminal component.
- Learned the difference between default exports and named exports in JavaScript modules.
- Debugged React errors using `console.log()` and `Object.keys()`.

in cli.scss -> div[name="react-console-emulator"] {height: 100%;]

# Window Fullscreen Feature
- Added fullscreen functionality using React useState.
const [fullscreen, setFullscreen] = useState(false);
- Implemented green window control button to toggle fullscreen mode.
- Replaced Rnd default sizing behavior with controlled size and position props.
<Rnd size={{  width: fullscreen ? "100vw" : width, height: fullscreen ? "100vh" : height}}
  position={{ x: fullscreen ? 0 : 50, y: fullscreen ? 0 : 50}} > --> controlled component